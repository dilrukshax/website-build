{
    "name": "@booking-engine/booking-engine",
    "version": "1.0.0",
    "private": true,
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc --watch",
        "clean": "rimraf dist"
    },
    "dependencies": {
        "@booking-engine/core": "workspace:*",
        "@booking-engine/database": "workspace:*",
        "@booking-engine/auth": "workspace:*"
    },
    "devDependencies": {
        "@booking-engine/config": "workspace:*",
        "typescript": "^5.3.0",
        "rimraf": "^5.0.0"
    }
}